-- 데이터베이스 초기화 스크립트
-- 이 파일은 애플리케이션 시작 시 자동으로 실행됩니다.

-- 기존 테이블 삭제 (필요한 경우)
-- DROP TABLE IF EXISTS offer CASCADE;
-- DROP TABLE IF EXISTS working CASCADE;
-- DROP TABLE IF EXISTS site_user CASCADE;

-- 테이블이 자동으로 생성되므로 여기서는 샘플 데이터만 삽입
-- 실제 데이터는 애플리케이션에서 관리자 계정 생성 시 자동으로 생성됩니다. 